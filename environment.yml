name: pg_amf
channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.11
  - matplotlib>=3.7
  - scikit-learn>=1.3
  - pyyaml>=6.0
  - tqdm>=4.65
  - pytest>=7.4
  # PyTorch — CPU-only default; for CUDA change to pytorch-cuda=12.1
  - pytorch>=2.0
  - cpuonly
